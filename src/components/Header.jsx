import React from "react";

const Header = () => {
    return (
        <div
            style={{
                backgroundColor: "#FFC107",
                padding: "10px",
                textAlign: "center",
            }}
        >
            Breakout rooms are in session
        </div>
    );
};

export default Header;
